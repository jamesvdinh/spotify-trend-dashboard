# Airflow's own runtime - kept separate from backend/requirements.txt.
# dbt-bigquery for the dbt tasks; the rest so BashOperator can shell out to
# backend/ingestion/*.py (spotify_snapshot.py, kworb_scraper.py) in the same
# container, matching what those scripts already need to run standalone.
dbt-bigquery>=1.8
redis>=5.0
httpx>=0.27
pydantic-settings>=2.5
google-cloud-bigquery>=3.25
beautifulsoup4>=4.12
