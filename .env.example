# Compose-level variables (used for substitution in docker-compose.yml itself,
# separate from backend/.env which the poller container loads for app settings).
BQ_PROJECT_ID=spotify-trend-dashboard
GOOGLE_APPLICATION_CREDENTIALS_HOST=/Users/you/.config/gcloud/keys/spotify-trend-dashboard-dbt-ingest.json
